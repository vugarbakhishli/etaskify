server:
  port: ${API_SERVER_PORT:8080}
  servlet:
    application-display-name: user-ms

info:
  app:
    name: "${project.name}"
    description: "${project.description}"
    version: "${project.version}"
    java:
      version: "${java.version}"
